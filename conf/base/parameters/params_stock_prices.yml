stock_prices:
  start_date: "2023-01-01"
  batch_size: 100  # tickers per yfinance download call
  index_sources:
    sp500: true
    nasdaq100: true
  # Extra tickers always fetched alongside the index universe (e.g. benchmarks).
  extra_tickers:
    - "SPY"
